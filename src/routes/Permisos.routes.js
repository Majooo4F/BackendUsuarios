// import express from "express";
// import { getPermisos, seedPermisos } from "../controllers/Permisos.controller.js";
// import { verifyToken } from "../middlewares/auth.middleware.js";

// const router = express.Router();

// // 🔹 GET /permisos → traer todos los permisos
// router.get("/", verifyToken, getPermisos);

// // 🔹 POST /permisos/seed → insertar permisos iniciales
// router.post("/seed", verifyToken, seedPermisos);

// export default router;